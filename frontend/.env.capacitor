# Fill in the real Render service hostname before the next native build —
# render.yaml's `name` isn't authoritative since this service doesn't read
# that file; get the actual hostname from the Render dashboard.
VITE_API_URL=https://fitsense-ai-backend.onrender.com
